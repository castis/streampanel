windows:
  - name: streaming-panel
    root: ~/Code/streaming-panel
    layout: main-vertical
    panes:
      - name: workspace
        commands:
          - git st
      - name: flask
        commands:
          - poetry run python app.py
      - name: webpack
        commands:
          - nvm use
          - npm run dev
