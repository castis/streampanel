windows:
  - name: streaming-panel
    root: ~/Code/streaming-panel
    layout: main-vertical
    panes:
      - name: workspace
        commands:
          - git st
      - name: serve
        commands:
          - nvm use
          - npx serve public -s
      - name: webpack
        commands:
          - nvm use
          - npm run dev
