windows:
  - name: streaming-panel
    root: ~/Code/streaming-panel
    layout: main-vertical
    panes:
      - name: workspace
        commands:
          - git st
      - name: flask
        commands:
          - poetry run flask run
      - name: webpack
        commands:
          - nvm use
          - npm run dev
